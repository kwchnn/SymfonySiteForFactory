<!DOCTYPE html>

<title>New ItemCategory</title>

{% block body %}
    <h1>Create new ItemCategory</h1>

    {{ include('item_category/_form.html.twig') }}

    <a href="{{ path('app_item_category_index') }}">back to list</a>
{% endblock %}
